name: Angular Pipeline

on:
  push:
    branches: [main, develop]
    paths:
      - 'frontend/looker/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'frontend/looker/**'

jobs:
  build-angular:
    runs-on: self-hosted
    timeout-minutes: 30

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v4

      - name: Setup Node
        id: setup-node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'frontend/looker/package-lock.json'

      - name: Install dependencies
        id: install
        working-directory: frontend/looker
        run: npm ci

      - name: Lint
        id: lint
        working-directory: frontend/looker
        run: npm run lint

      - name: Test
        id: test
        working-directory: frontend/looker
        run: npm run test -- --watch=false --browsers=ChromeHeadless --code-coverage

      - name: Upload test results
        id: upload-test-results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: angular-test-coverage
          path: frontend/looker/coverage/

      - name: SonarQube Scan
        id: sonarqube
        uses: sonarsource/sonarqube-scan-action@v5
        with:
          projectBaseDir: frontend/looker
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: Build production
        id: build-prod
        working-directory: frontend/looker
        run: npm run build -- --configuration production
